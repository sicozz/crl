syntax = "proto3";
option go_package = "github.com/sicozz/crl/api/v0/crl";

service Greeter {
    rpc Hello(Name) returns (Greeting);
}

message Name {
    string name = 1;
}

message Greeting {
    string greeting = 1;
}
